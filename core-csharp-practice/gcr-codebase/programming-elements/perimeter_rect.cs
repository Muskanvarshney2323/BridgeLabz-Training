using System;

class Program
{
    static void Main()
    {
        double length = 10;
        double width = 5;

        double perimeter = 2 * (length + width);

        Console.WriteLine("Perimeter of Rectangle = " + perimeter);
    }
}
